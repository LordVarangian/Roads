namespace = mod_roads
country_event = {
    id = mod_roads.1
    title = debug
    desc = debug
    is_triggered_only = yes
    hidden = yes
    option = {
        road_recalculate_country_immediate = yes
    }
}
country_event = {
    id = mod_roads.2
    title = mod_roads.2_t
    desc = mod_roads.2_d
    picture = mod_roads.2_p

    is_triggered_only = yes

    option = {
        name = mod_roads.2_o1
        hidden_effect = {
            FROM = { set_variable = { which =  building_menu_temple_yearly_tax_dev_prov value = 0 } }
 			region = {
                limit = {
                    owned_by = FROM
                    area_for_scope_province = {
                        owned_by = FROM
                        OR = { has_building = temple has_building = cathedral }
                    }
                }
                export_to_variable = { which = building_menu_temple_yearly_tax_dev_prov value = base_tax }
                FROM = {
                    change_variable = { which = building_menu_temple_yearly_tax_dev_prov which = PREV }
                }
            }
				scaled_effect_full = { effect = add_treasury which = building_menu_temple_yearly_tax_dev_prov }
                while = {
                    limit = { check_variable = { which = building_menu_temple_yearly_tax_dev_prov value = 1 } }
                    subtract_variable = { which = building_menu_temple_yearly_tax_dev_prov value = 1 }
                    FROM = { add_treasury = 3 change_variable = { which = building_menu_temple_yearly_tax_cash value = 3 } }
                }
            }
        }
    }
}